@using abw.ViewModels
@model Grid<CarForDisplay>
@{
	ViewBag.Title = "Машины";
}
@{ Html.RenderPartial("Buttons/_New"); }
<table class="table table-striped table-bordered grid">
	<thead>
		<tr>
			<th>Марка</th>
			<th>Модели</th>
			<th></th>
		</tr>
	</thead>
	<tbody>
		@foreach (CarForDisplay car in Model.List)
		{
			<tr>
				<td>
					@Html.ActionLink(car.Make, "Edit", new { id = car.Id }, new { title = "Edit" })
				</td>
				<td>
					@foreach (string carModel in car.Models)
					{
						<div>@carModel</div>
					}
				</td>
				<td title="Удалить">
					<i class="glyphicon glyphicon-remove" title="Удалить"></i>
				</td>
			</tr>
		}
		@if (Model.List.Count == 0)
		{
			Html.RenderPartial("_NoRecordsFound", 3);
		}
	</tbody>
</table>
