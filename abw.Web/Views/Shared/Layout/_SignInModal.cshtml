@using abw.ViewModels
@model SignInModel
<div data-bind="modal: signInModalIsOpened" id="signInModal" class="modal fade" tabindex="-1" role="dialog" aria-hidden="true">
	<div class="modal-dialog">
		@using (Html.BeginForm("SignIn", "Account", FormMethod.Post, new { @class = "modal-content" }))
		{
			<div class="modal-header">
				<button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
				<h4 class="modal-title">Войти</h4>
			</div>
			<div class="modal-body">
				<div>
					@Html.LabelFor(m => m.Name)
					@Html.TextBoxFor(m => m.Name, new { autocomplete = "off" })
					@Html.ValidationMessageFor(m => m.Name)
				</div>

				<div>
					@Html.LabelFor(m => m.Password)
					@Html.PasswordFor(m => m.Password)
					@Html.ValidationMessageFor(m => m.Password)
				</div>
			</div>
			<div class="modal-footer">
				<input type="submit" class="btn btn-primary cancel" value="Войти" />
			</div>
		}
	</div>
</div>